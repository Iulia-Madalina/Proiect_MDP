@page
@model Proiect_MDP.Pages.Flowers.CreateModel

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Flower</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Flower.Name" class="control-label"></label>
                <input asp-for="Flower.Name" class="form-control" />
                <span asp-validation-for="Flower.Name" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Flower.Bio" class="control-label"></label>
                <input asp-for="Flower.Bio" class="form-control" />
                <span asp-validation-for="Flower.Bio" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Flower.Price" class="control-label"></label>
                <input asp-for="Flower.Price" class="form-control" />
                <span asp-validation-for="Flower.Price" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Flower.MaturingDate" class="control-label"></label>
                <input asp-for="Flower.MaturingDate" class="form-control" />
                <span asp-validation-for="Flower.MaturingDate" class="textdanger"></span>
            </div>
            <div>
                <label asp-for="Flower.SellerID" class="control-label">Seller Name</label>
                <select asp-for="Flower.SellerID" class="form-control" asp-items="ViewBag.SellerID">
                </select>
                <span asp-validation-for="Flower.SellerID" class="text-danger"></span>
            </div>
            <div class="form-group">
                <div class="table">
                    <table>
                        <tr>
                            @{
                                int cnt = 0;
                                foreach (var cat in Model.AssignedCategoryDataList)
                                {
                                    if (cnt++ % 3 == 0)
                                    {
                                    @:</tr><tr>
                                    }
                                    @:<td>
                                        <input type="checkbox"
                                               name="selectedCategories"
                                               value="@cat.CategoryID"
                                               @(Html.Raw(cat.Assigned ? "checked=\"checked\"" : "")) />
                                        @cat.CategoryID @: @cat.Name
                                    @:</td>
                                }
                            @:</tr>
                            }
                        </table>
                    </div>
                </div>
                <div class="form-group">
                    <input type="submit" value="Create" class="btn btn-primary" />
                </div>
            </form>
    </div>
</div>

<div>
    <a asp-page="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
